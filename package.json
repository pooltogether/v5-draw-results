{
  "name": "v5-draw-results",
  "version": "1.0.0",
  "description": "Periodically runs code to lookup winners for each draw and store the results.",
  "main": "index.js",
  "repository": "git@github.com:pooltogether/v5-draw-results.git",
  "author": "PoolTogether Inc.",
  "license": "MIT",
  "scripts": {
    "pwd": "echo ${PWD}"
  },
  "dependencies": {
    "@actions/core": "1.9.0",
    "@pooltogether/v5-cli": "^0.0.1-beta.1",
    "typescript": "^5.0.4"
  }
}